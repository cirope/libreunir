<% if @tag.errors.empty? %>
  Turbolinks.visit(window.location.pathname);
<% else %>
  $('[data-tags-new]').find('form').replaceWith(
    '<%= j render(template: 'tags/edit', formats: [:html]) %>'
  );
  $('#tag_name').focus();
<% end %>
