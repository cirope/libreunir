$('[data-schedule-modal]').html(
  '<%= j render(template: 'schedules/schedule_modal',
    locals: {
      header: (@schedule.schedulable ? @schedule.label : ''),
      body: render('form', formats: [:html]),
      cancel: false
    }
  ) %>'
);
<%= render 'datepicker_setup' %>
