$('[data-loan-info]').remove();

$('#loan-info-link-<%= @loan.to_param %>').closest('tr').after(
  $('<tr id="loan-info-<%= @loan.to_param %>" data-loan-info></tr>').html(
    $('<td colspan="7"></td>').html(
      '<%= j render(template: 'loans/show', formats: [:html]) %>'
    )
  )
);
