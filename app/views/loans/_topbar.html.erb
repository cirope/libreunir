<ul class="nav nav-pills">
  <li class="dropdown active">
    <a class="dropdown-toggle" id="navtags" role="button" data-toggle="dropdown" href="#">
      <%= content_tag(:span, '&#xe02b;'.html_safe, class: 'iconic') << ' ' << Tag.model_name.human(count: 0) %>
      <b class="caret"></b>
    </a>
    <ul class="dropdown-menu" aria-labelledby="navtags">
      <% current_user.tags.each do |tag|  %>
        <li><%= link_to tag.name, '#', data: { tag_id: tag.id } %></li>
      <% end %>
      <li class="divider"></li>
      <li><%= link_to t('view.tags.new'), new_tag_path, data: { remote: true } %></li>
    </ul>
  </li> 
  <li class="active hidden">
    <%= link_to 'Eliminar', '#', id: :destroy_tagging %>
  </li>
</ul>
