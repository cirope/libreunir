<blockquote>
  <p><%= note.note %></p>
  <small>
    <%= t(
      'view.notes.created_by.html',
      user: note.user.to_s,
      distance_time: distance_of_time_in_words_to_now(note.created_at)
    ) %>
  </small>
</blockquote>
